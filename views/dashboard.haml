%a{ href: "/logout" }
  ログアウトする
%form{ method: "post", action: "/-/post" }
  %input{ type: "text", name: "title" }
  %textarea{ name: "body" }
  %input{ type: "submit" }

- @notes.each do |notes|
  %h3 #{notes.title}
  %div= markdown(notes.body)